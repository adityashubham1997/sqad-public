# SQAD-Public Configuration — generated by /setup
# Run /setup to configure, or edit manually.
# Version: 1.0

# --- Company & Project ---
company:
  name: ""
  domain: ""                    # e.g., fintech, healthcare, e-commerce
  compliance: []                # [SOC2, HIPAA, PCI, GDPR]

project:
  name: ""
  description: ""
  type: ""                      # web-app | api | library | cli | mobile | infra

# --- Team & User ---
team:
  name: ""
user:
  name: ""
  role: ""

# --- Git Host ---
github:
  host: "github.com"
  org: ""
  default_branch: "main"

# --- Stack (auto-detected by /setup) ---
stack:
  languages: []                 # [typescript, python, java, go, rust, ruby, csharp]
  frameworks: []                # [react, nextjs, express, django, spring, fastapi]
  build_tools: []               # [npm, yarn, pnpm, maven, gradle, cargo, pip]
  test_frameworks: []           # [jest, mocha, pytest, junit, go-test]
  test_command: "npm test"
  build_command: ""
  lint_command: "npm run lint"

# --- Cloud (auto-detected by /setup) ---
cloud:
  providers: []
  iac: []
  container: []
  ci_cd: []
  monitoring: []                # [datadog, prometheus, cloudwatch, grafana]

# --- Issue Tracker ---
tracker:
  type: ""                      # jira | linear | shortcut | notion | github-issues | none
  project_key: ""
  api_url: ""
  mcp_available: false

# --- Context Files ---
context_files:
  canonical: "CONTEXT.md"
  copies: ["CLAUDE.md", "GEMINI.md", "AGENTS.md"]

# --- Repos (populated by /refresh) ---
repos: []

# --- Knowledge Graph ---
knowledge_graph:
  enabled: true
  auto_rebuild: true
  include_in_context_file: true

# --- Agents ---
agents:
  built_in: 17
  custom: []

# --- Plugins ---
plugins: []

# --- IDEs ---
ides:
  installed: ["claude", "windsurf", "cursor"]

# --- Communication ---
communication_language: "English"
